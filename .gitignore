node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
# Onboarding bijlagen uploads
uploads/onboarding-bijlagen/
.replit
# Lokale JSON-database/seed data — mag nooit gecommit worden (bevat eventueel wachtwoorden)
server/db.json
# Environment variabelen / secrets — nooit committen
.env
.env.local
.env.*.local
*.env
.env.production
# Cookie-bestanden bevatten sessie-tokens — nooit committen
cookies.txt
# Patchbestanden zijn overdrachtsmiddel, geen broncode: ze worden aangeleverd,
# met `git am` toegepast en zijn daarna overbodig. Belandden ze tóch in een
# commit, dan groeide elke `git pull` met duizenden regels die niets met de
# site te maken hebben.
*.patch
